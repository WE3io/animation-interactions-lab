---
import StackedCardsSection from "../../interactions/stacked-cards/StackedCardsSection.astro";

const cards = [
  {
    id: "card-1",
    title: "Discovery",
    body: "Gather implementation constraints and define what must remain visible during scroll."
  },
  {
    id: "card-2",
    title: "Design",
    body: "Model the stacked-state transitions before wiring scroll-driven animation."
  },
  {
    id: "card-3",
    title: "Build",
    body: "Implement the reusable component and keep interaction scoped to each instance."
  },
  {
    id: "card-4",
    title: "Verify",
    body: "Validate reduced motion, responsive layout, and lifecycle cleanup behavior."
  }
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Astro Stacked Cards Demo</title>
    <style>
      body {
        margin: 0;
        font-family: "IBM Plex Sans", "Segoe UI", sans-serif;
        color: #0f172a;
        background: linear-gradient(180deg, #f8fafc 0%, #eef2ff 100%);
      }

      main {
        max-width: 72rem;
        margin: 0 auto;
        padding: 2rem 1rem 8rem;
      }

      .spacer {
        min-height: 40vh;
      }
    </style>
  </head>
  <body>
    <main>
      <div class="spacer"></div>

      <StackedCardsSection
        title="Pinned Context with Stacked Scroll Cards"
        cards={cards}
        animation={{ stepDuration: 0.5 }}
      >
        <div slot="pinned">
          <h3>Persistent Context</h3>
          <p>
            The left column stays sticky while the right column animates card depth based on scroll
            progress.
          </p>
          <p>
            Reduced-motion users and small screens fall back to a static, readable layout.
          </p>
        </div>
      </StackedCardsSection>

      <div class="spacer"></div>
    </main>
  </body>
</html>
